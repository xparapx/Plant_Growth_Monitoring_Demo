name: pages

# docs/ (project overview + manual) stays at the site root; the mock-data demo of the web UI
# is published under /app/.  One-time: Settings -> Pages -> Source = "GitHub Actions".
on:
  push:
    branches: [main]
    paths: ["docs/**", "web/**", ".github/workflows/pages.yml"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: web/package-lock.json
      - run: mkdir -p _site && cp -r docs/. _site/ && touch _site/.nojekyll
      - working-directory: web
        run: |
          npm ci
          npm run build:pages
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
